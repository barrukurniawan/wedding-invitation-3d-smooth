# HTTP → HTTPS redirect + ACME challenge
server {
    listen 80 default_server;
    server_name _;
    return 444;
}

server {
    listen 80;
    server_name marryme.web.id *.marryme.web.id;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name marryme.web.id *.marryme.web.id;

    ssl_certificate     /etc/letsencrypt/live/marryme.web.id/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/marryme.web.id/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 1d;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options nosniff always;
    # Tenant previews are embedded by the root dashboard from a different
    # subdomain. CSP is the supported framing policy; unlike X-Frame-Options
    # SAMEORIGIN it can explicitly allow the trusted dashboard origin.
    add_header Content-Security-Policy "frame-ancestors 'self' https://marryme.web.id http://localhost:5173 http://127.0.0.1:5173" always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;

    root /usr/share/nginx/html;
    index index.html;
    client_max_body_size 20M;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_min_length 1024;
    gzip_comp_level 5;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/xml
        application/wasm
        application/octet-stream
        model/gltf+json
        model/gltf-binary
        image/svg+xml
        font/woff2;

    # API media URLs can end in .webp/.mp3; ^~ keeps them out of the static
    # extension regex locations below.
    location ^~ /api/ {
        proxy_pass http://api:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Revalidate the HTML shell so deploys can publish a new asset manifest.
    # Fingerprinted JS/CSS below remains immutable and long-lived.
    location = /index.html {
        try_files $uri =404;
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Hashed build assets (Vite) — long cache OK
    location ~* \.(?:css|js|mjs|png|jpg|jpeg|gif|svg|webp|woff2|mp3)$ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Non-hashed 3D models — shorter cache so model updates are not stuck for 1y
    location ~* \.(?:gltf|glb|bin)$ {
        try_files $uri =404;
        expires 7d;
        add_header Cache-Control "public";
    }
}
