# Used by docker-compose.yml (project root). Do not commit real values.
MYSQL_ROOT_PASSWORD=__REPLACE_ROOT_PASSWORD__
MYSQL_PASSWORD=__REPLACE_DB_PASSWORD_SAME_AS_SERVER_ENV__
PAYMENT_MODE=manual
INVITATION_PRICE_IDR=0
