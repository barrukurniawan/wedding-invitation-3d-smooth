import type { Scene } from 'three';
export declare const useEnvironment: (scene: () => Scene, environment: () => Scene["environment"] | undefined, isBackground: () => boolean, isEnvironment: () => boolean) => void;
